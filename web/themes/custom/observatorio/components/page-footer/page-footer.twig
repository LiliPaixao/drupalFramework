{#
/**
 * @file
 * Template for a Page Footer component.
 *
#}
{% set footer_attributes = footer_attributes ?: create_attribute() %}
{%
  set footer_classes = [
    'page__footer',
  ]|merge(footer_utility_classes ?: [])
%}

{% if page.footer %}
  <footer {{ footer_attributes.addClass(footer_classes) }}>
    <div class="container">
      <div class="d-flex flex-wrap justify-content-md-between align-items-md-center">
        {% block page_inner_footer %}
          {{ page.footer }}
        {% endblock %}
      </div>
    </div>
  </footer>
{% endif %}

{# {% set footer_attributes = footer_attributes ?: create_attribute() %}
{%
  set footer_classes = [
    'page__footer',
  ]|merge(footer_utility_classes ?: [])
%}

{% if page.footer %}
  <footer {{ footer_attributes.addClass(footer_classes) }}>
    <div class="container">
      <div class="row">
        <div class="col-md-4 mb-3">
          {% if page.footer.observatorio_footerleft %}
            {{page.footer.observatorio_footerleft }}
          {% endif %}
        </div>
        <div class="col-md-4 mb-3">
          {% if page.footer.observatorio_footercenter %}
            {{ page.footer.observatorio_footercenter }}
          {% endif %}
        </div>
        <div class="col-md-4 mb-4">
          {% if page.footer.observatorio_footerright %}
            {{ page.footer.observatorio_footerright }}
          {% endif %}
        </div>
      </div>
    </div>
  </footer>
{% endif %}           #}
